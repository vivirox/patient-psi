---
import Layout from '../../../../layouts/Layout.astro';
import ChatWindow from '../../../../components/chat/ChatWindow';
import { getUserFromRequest } from '../../../../lib/auth';

const userId = await getUserFromRequest(Astro.request);
if (!userId) {
  return Astro.redirect('/login');
}

const { patientId, chatId } = Astro.params;
---

<Layout title="Chat - Patient PSI">
  <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    <div class="px-4 py-6 sm:px-0">
      <div class="mb-6">
        <a
          href={`/patients/${patientId}/chats`}
          class="text-indigo-600 hover:text-indigo-900"
        >
          ← Back to Chats
        </a>
      </div>
      <ChatWindow client:load patientId={patientId} chatId={chatId} />
    </div>
  </div>
</Layout>
